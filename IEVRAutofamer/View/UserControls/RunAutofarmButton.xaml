<UserControl x:Class="IEVRAutofamer.View.UserControls.RunAutofarmButton"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:IEVRAutofamer.View.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="200" d:DesignWidth="500"
             x:Name="root">

    <UserControl.Resources>
        <Style TargetType="Image">
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">

                    <Trigger.EnterActions>
                        <BeginStoryboard>
                            <Storyboard>
                                <DoubleAnimation Storyboard.TargetProperty="Width"
                                                 To="460"
                                                 Duration="0:0:0.2"/>
                                <DoubleAnimation Storyboard.TargetProperty="Height"
                                                 To="130"
                                                 Duration="0:0:0.2"/>
                            </Storyboard>
                        </BeginStoryboard>
                    </Trigger.EnterActions>

                    <Trigger.ExitActions>
                        <BeginStoryboard>
                            <Storyboard>
                                <DoubleAnimation Storyboard.TargetProperty="Width"
                                                 To="440"
                                                 Duration="0:0:0.1"/>
                                <DoubleAnimation Storyboard.TargetProperty="Height"
                                                 To="120"
                                                 Duration="0:0:0.1"/>
                            </Storyboard>
                        </BeginStoryboard>
                    </Trigger.ExitActions>

                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Button HorizontalAlignment="Center" VerticalAlignment="Center"
                Background="Transparent" BorderBrush="Transparent"
                Command="{Binding Command, ElementName=root}">
            <Image x:Name="runImage" Source="{Binding ImageSource}"
                   Width="440"
                   Height="120"/>
        </Button>

        <Label x:Name="buttonText"
               Content="Run Autofarm"
               Foreground="#FFD8D8D8"
               VerticalAlignment="Center"
               HorizontalAlignment="Center"
               FontSize="22"
               FontWeight="Bold"
               IsHitTestVisible="False"/>
    </Grid>

</UserControl>